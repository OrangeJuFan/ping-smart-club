# Ping Smart Club Front (Uni-app)

> 乒乓智能俱乐部前端应用 - 基于 Uni-app 框架的多端统一解决方案

## ✨ 项目简介

Ping Smart Club Front 是乒乓智能俱乐部系统的移动端应用，基于 Uni-app 框架开发，支持多端部署（微信小程序、H5、Android/iOS App）。采用Vue 2 + Options API + Vuex的经典技术栈，为俱乐部成员提供便捷的课程预约和管理服务。

## 🛠️ 技术栈

### 核心框架
- **Uni-app 3.x** - 跨平台应用开发框架
- **Vue 2.7** - 渐进式 JavaScript 框架
- **Vuex 3.x** - Vue 官方状态管理库

### 开发工具
- **SCSS** - CSS 预处理器
- **ESLint** - JavaScript 代码检查工具

### 跨端支持
- **微信小程序** - 主要部署平台
- **H5** - 浏览器访问
- **App** - Android/iOS 原生应用

## 📋 环境要求

- **Node.js** >= 16.0.0
- **npm** >= 8.0.0 或 **yarn** >= 1.22.0
- **微信开发者工具** (开发微信小程序时需要)

## 🚀 快速开始

### 1. 安装依赖

```bash
cd ping-smart-club-front
npm install
```

### 2. 开发环境运行

```bash
# 微信小程序开发
npm run dev:mp-weixin

# H5开发
npm run dev:h5

# App开发
npm run dev:app
```

### 3. 生产构建

```bash
# 微信小程序构建
npm run build:mp-weixin

# H5构建
npm run build:h5

# App构建
npm run build:app
```

## 📁 项目结构

```
ping-smart-club-front/
├── api/                          # API 接口管理
│   ├── index.js                 # API 主入口，统一导出
│   └── modules/                 # API 模块（按业务划分）
│       ├── user.js              # 用户相关接口
│       ├── coach.js             # 教练相关接口
│       ├── admin.js             # 管理员相关接口
│       └── common.js            # 公共接口
├── components/                   # 组件目录
│   ├── common/                  # 通用组件库
│   │   ├── btn.vue             # 自定义按钮组件
│   │   ├── card.vue            # 卡片组件
│   │   ├── empty.vue           # 空状态组件
│   │   ├── loading.vue         # 加载状态组件
│   │   └── ...
│   └── components/              # 页面级组件
│       ├── custom-tabbar.vue   # 自定义底部导航
│       └── login-modal.vue     # 登录弹窗组件
├── pages/                        # 页面目录（按角色划分）
│   ├── index/                   # 首页
│   ├── login/                   # 登录页面
│   ├── identity/                # 身份选择
│   ├── club/                    # 俱乐部首页
│   ├── parent/                  # 家长端页面
│   │   ├── parent.vue          # 家长首页
│   │   ├── booking.vue         # 约课页面
│   │   └── my.vue              # 家长个人中心
│   ├── coach/                   # 教练端页面
│   │   ├── coach.vue           # 教练首页
│   │   ├── course.vue          # 课程管理
│   │   └── my.vue              # 教练个人中心
│   ├── admin/                   # 管理员页面
│   │   ├── admin.vue           # 管理员首页
│   │   ├── manage.vue          # 系统管理
│   │   └── my.vue              # 管理员个人中心
│   └── components/              # 页面组件（复用的页面级组件）
├── static/                       # 静态资源
│   ├── images/                  # 图片资源
│   └── logo.png                 # 应用图标
├── store/                        # Vuex 状态管理
│   ├── index.js                 # Store 主入口
│   └── modules/                 # 状态模块
│       ├── user.js              # 用户状态
│       ├── app.js               # 应用全局状态
│       ├── coach.js             # 教练状态
│       └── admin.js             # 管理员状态
├── utils/                        # 工具函数库
│   ├── index.js                 # 通用工具函数
│   └── request.js               # HTTP 请求封装
├── App.vue                       # 应用根组件
├── main.js                       # 应用入口文件
├── manifest.json                 # 应用配置
├── pages.json                    # 页面路由配置
├── uni.scss                      # 全局样式
└── uni.promisify.adaptor.js      # Promise 适配器
```

## 🔧 开发规范

### 组件开发
- **强制使用 Options API** - Vue 2 标准写法
- **禁止使用 Composition API** - 保持代码一致性
- 遵循 `<template>` -> `<script>` -> `<style scoped>` 的顺序
- 组件名使用 PascalCase 命名

### 组件规范
```vue
<template>
  <!-- 模板内容 -->
</template>

<script>
export default {
  name: 'ComponentName',
  props: {
    // 属性定义
  },
  data() {
    return {
      // 数据定义
    }
  },
  computed: {
    // 计算属性
  },
  methods: {
    // 方法定义
  },
  // 生命周期钩子
  mounted() {
    // 组件挂载后执行
  }
}
</script>

<style scoped lang="scss">
// 样式定义
</style>
```

### 标签使用规范
- **严禁使用 HTML 原生标签** (`<div>`, `<span>`, `<img>`)
- **必须使用 Uni-app 原生组件** (`<view>`, `<text>`, `<image>`)
- 布局强制使用 Flexbox
- 响应式单位统一使用 `rpx`

### 生命周期使用
- 页面级组件优先使用 `onLoad` 获取路由参数
- 避免在 `data` 中放置大型对象
- 异步操作使用 `uni.` 原生 API

### 状态管理
- 全局状态通过 Vuex 管理
- 组件内部状态使用 `data`
- 复杂状态逻辑抽离到 Vuex actions

### API 调用规范
- **禁止在组件内直接使用 `uni.request`**
- 所有 API 请求必须通过 `this.$api.xxx()` 调用
- API 按业务模块组织在 `api/modules/` 目录

### 样式规范
- 使用 SCSS 作为预处理器
- 全局样式定义在 `uni.scss`
- 组件样式使用 `scoped`
- 样式穿透使用 `::v-deep` 或 `/deep/`

## 🌐 页面路由说明

### 角色页面划分
- **游客页面**: `index`, `login`, `identity` - 未登录用户可访问
- **家长页面**: `parent/*` - 学员家长专用页面
- **教练页面**: `coach/*` - 教练专用页面
- **管理员页面**: `admin/*` - 管理员专用页面

### 底部导航配置
```json
{
  "tabBar": {
    "custom": true,
    "list": [
      { "pagePath": "pages/club/club", "text": "首页" },
      { "pagePath": "pages/parent/booking", "text": "约课" },
      { "pagePath": "pages/parent/my", "text": "我的" },
      { "pagePath": "pages/coach/course", "text": "课程" },
      { "pagePath": "pages/coach/my", "text": "我的" },
      { "pagePath": "pages/admin/manage", "text": "管理" },
      { "pagePath": "pages/admin/my", "text": "我的" }
    ]
  }
}
```

## 📜 可用脚本命令

| 命令 | 平台 | 描述 |
|------|------|------|
| `npm run dev:mp-weixin` | 微信小程序 | 启动微信小程序开发环境 |
| `npm run dev:h5` | H5 | 启动浏览器开发环境 |
| `npm run dev:app` | App | 启动App开发环境 |
| `npm run build:mp-weixin` | 微信小程序 | 构建微信小程序生产版本 |
| `npm run build:h5` | H5 | 构建H5生产版本 |
| `npm run build:app` | App | 构建App生产版本 |

## 🔧 核心功能模块

### 用户系统
- **身份选择**: 家长/教练/管理员角色选择
- **登录注册**: 支持手机号验证码登录
- **权限控制**: 基于角色的页面访问控制

### 家长端功能
- **课程浏览**: 查看俱乐部课程安排
- **在线约课**: 预约教练课程
- **我的预约**: 管理个人预约记录
- **个人中心**: 个人信息管理

### 教练端功能
- **课程管理**: 创建和管理课程
- **学员管理**: 查看学员信息和预约
- **课程签到**: 课程开始签到管理
- **个人中心**: 教练信息管理

### 管理员功能
- **用户管理**: 用户信息和权限管理
- **课程审核**: 课程发布审核
- **数据统计**: 系统数据报表
- **系统设置**: 全局配置管理

## 🌐 跨端兼容性

### 微信小程序
- 主力开发和部署平台
- 支持微信支付、分享等原生能力

### H5版本
- 浏览器兼容访问
- 支持响应式布局
- 部分小程序专属功能降级处理

### App版本
- Android/iOS 原生应用
- 支持推送通知、设备权限等

## 📦 部署说明

### 微信小程序
1. 运行 `npm run build:mp-weixin`
2. 使用微信开发者工具导入 `unpackage/dist/dev/mp-weixin`
3. 提交审核并发布

### H5部署
1. 运行 `npm run build:h5`
2. 将 `unpackage/dist/build/h5` 部署到Web服务器
3. 配置Nginx反向代理（可选）

### App打包
1. 运行 `npm run build:app`
2. 使用HBuilderX或CLI进行云打包
3. 下载安装包并发布到应用市场

## ⚠️ 注意事项

1. **组件标签**: 必须使用Uni-app组件，不能使用HTML标签
2. **单位使用**: 布局统一使用`rpx`，避免使用`px`
3. **API调用**: 统一使用`this.$api`调用接口
4. **状态管理**: 全局状态通过Vuex管理
5. **跨端测试**: 不同平台功能需要分别测试
6. **性能优化**: 注意包体积和首屏加载速度

## 🔄 更新日志

### v1.0.0 (2025-01-19)
- ✅ 完成项目基础架构搭建
- ✅ 集成Vuex状态管理
- ✅ 封装HTTP请求模块
- ✅ 创建通用组件库
- ✅ 搭建页面基础结构

## 👥 开发团队

**Ping Smart Club Team** - 让乒乓球俱乐部管理更智能！🏓

---

如有问题或建议，请联系开发团队。